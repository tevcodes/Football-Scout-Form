rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 🟢 ALLOW EVERYTHING (For Development/Portfolio)
      allow read, write: if true;
    }
  }
}
